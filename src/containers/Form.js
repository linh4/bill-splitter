import React from 'react';
// import {connect} from 'react-redux'

const Form = (props) => {
    return (
      <div>
      <form>
         <input name="item"/>
         <button type="submit" value="submit">Save</button>
      </form>
      </div>
    )
}

export default Form
